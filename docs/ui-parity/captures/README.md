# Local UI parity captures

Place raw official captures in `official/` and corresponding CrystalOTC
captures in `crystal/`. Capture files and metadata are ignored because they may
contain character/account information. Accepted sanitized references can be
added deliberately later.

See `../screenshot-capture.md` for scenario names and capture requirements.
